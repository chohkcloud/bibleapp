# Ollama Proxy Configuration
# Copy this file to .env and set your own values

# ============================================
# API Key for authentication
# ============================================
# This key must be included in the X-API-Key header for all API requests
# IMPORTANT: Use a strong, random key of at least 32 characters
CHOCO_API_KEY=your-secret-api-key-here-change-this

# ============================================
# How to generate a secure API key:
# ============================================
#
# Option 1: PowerShell (Windows)
#   [System.Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
#
# Option 2: PowerShell (shorter command)
#   -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | ForEach-Object {[char]$_})
#
# Option 3: OpenSSL (if installed)
#   openssl rand -base64 32
#
# Option 4: Node.js
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Option 5: Python
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# Option 6: Online generator (not recommended for production)
#   https://randomkeygen.com/
#
# ============================================
# Example of a valid key (DO NOT USE THIS):
# CHOCO_API_KEY=xK9mP2nQ7rT4wY6zA3bC8dE1fG5hJ0kL
# ============================================
